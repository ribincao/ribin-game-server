serviceConfig:
  env: local
dbConfig:
  redisAddr: "localhost:6379"
  redisPasswd: ""
  redisMode: "client"
logConfig:
  logLevel: debug
  logPath: server.log
  logMode: combine
  logMaxAge: 3
  logMaxSize: 512